<!> - moments that need better solution
DEBUG - debug moments that should be deleted on production

- Связь с Response:
1. Отправка комментария с прикреплёнными файлами
2. Отображение комментария
3. Отображение прикреплённых файлов
4. Возможность одобрить/не одобрить ответ

- Досвязать с Project:
1. Завершить (-> модалка "Вы уверены, что хотите завершить...") / возобновить таску (/app/feed/[id] и /feed/[id]/task/[task_id]) - по нажатию на кнопку
2. Редактирование поста/таски (вёрстка по аналогии с вёрсткой редактора проектов /app/feed/[id])
3. Для владельцев поста: рядом с кнопкой "Редактировать" кнопка "Удалить" с красным текстом и изображением trash из public

- Связь с Feed:
1. Фильтры: новые, популярные, подписки
2. Фильтры по дате: с… и по… - удобное окошко с выбором даты
3. Видимость типа элемента в ленте: таска / пост / проект. Просто текстом

- Связь с Auth:
1. Изменение пароля


Make:
- feed/{project_id} - should be public page
- logout button
- avatar

ui/images/HeaderIcon:
- Replace stub by image

Отложенное не-MVP:
- ProfileInput tag in Project Editor color
- No "eye" on login inputs
- Подтверждение email - "Email успешно подтверждён!", кнопка: "Перейти к авторизации"
- Почта введена неправильно — почта меняется и повторно отправляется письмо (USE CASE)
- next-intl
- Понакидать user-select: none; где надо, чтобы нельзя было выделять текст
- Форма создания проекта: 1. Теги не по дизайну, 2. У названия есть красная *, у остальных полей - нет (то же самое в форме поста и таски)
- При автозаполнении почты в регистрации кнопка submit не пропускает
- Response/CommentForm - кнопка отправки должна иметь стиль disabled при невозможности отправки